name: Build with Nuitka

on:
  # push:
  #   branches:
  #     - next
  # pull_request:
  #   branches:
  #     - next
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install toml imageio nuitka
          pip install .

      - name: Build executable with Nuitka
        run: |
            python -m nuitka --onefile --assume-yes-for-downloads --include-package=aniworld,fake_useragent --output-filename=aniworld.exe `
            --company-name="Phoenixthrush UwU" `
            --product-name="AniWorld Downloader" `
            --file-version=$(python -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") `
            --product-version=$(python -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") `
            --file-description="AniWorld Downloader is a command-line tool built to download and stream anime content from aniworld.to" `
            --copyright="Copyright (c) 2024-$(python -c "import datetime; print(datetime.datetime.now().year)") phoenixthrush, Tmaster055" `
            --windows-icon-from-ico=src/aniworld/icon.webp `
            src/aniworld/entry.py

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: aniworld-windows
          path: aniworld.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install toml imageio nuitka
          pip install .

      - name: Build executable with Nuitka
        run: |
          python3 -m nuitka --onefile --assume-yes-for-downloads --include-package=aniworld,fake_useragent --output-filename=aniworld \
          --company-name="Phoenixthrush UwU" \
          --product-name="AniWorld Downloader" \
          --file-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --product-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --file-description="AniWorld Downloader is a command-line tool built to download and stream anime content from aniworld.to" \
          --copyright="Copyright (c) 2024-$(python3 -c "import datetime; print(datetime.datetime.now().year)") phoenixthrush, Tmaster055" \
          --linux-icon=src/aniworld/icon.webp \
          src/aniworld/entry.py

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: aniworld-linux
          path: aniworld

  build-macos-intel:
    runs-on: macos-13
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install toml imageio nuitka
          pip install .

      - name: Build executable with Nuitka
        run: |
          python3 -m nuitka --onefile --assume-yes-for-downloads --include-package=aniworld,fake_useragent --output-filename=aniworld \
          --company-name="Phoenixthrush UwU" \
          --product-name="AniWorld Downloader" \
          --file-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --product-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --file-description="AniWorld Downloader is a command-line tool built to download and stream anime content from aniworld.to" \
          --copyright="Copyright (c) 2024-$(python3 -c "import datetime; print(datetime.datetime.now().year)") phoenixthrush, Tmaster055" \
          --macos-app-icon=src/aniworld/icon.webp \
          --macos-signed-app-name=com.phoenixthrush.aniworld \
          src/aniworld/entry.py

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: aniworld-macos-intel
          path: aniworld

  build-macos-arm:
    runs-on: macos-13-arm64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install toml imageio nuitka
          pip install .

      - name: Build executable with Nuitka
        run: |
          python3 -m nuitka --onefile --assume-yes-for-downloads --include-package=aniworld,fake_useragent --output-filename=aniworld \
          --company-name="Phoenixthrush UwU" \
          --product-name="AniWorld Downloader" \
          --file-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --product-version=$(python3 -c "import toml; print(toml.load(open('pyproject.toml'))['project']['version'])") \
          --file-description="AniWorld Downloader is a command-line tool built to download and stream anime content from aniworld.to" \
          --copyright="Copyright (c) 2024-$(python3 -c "import datetime; print(datetime.datetime.now().year)") phoenixthrush, Tmaster055" \
          --macos-app-icon=src/aniworld/icon.webp \
          --macos-signed-app-name=com.phoenixthrush.aniworld \
          src/aniworld/entry.py

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: aniworld-macos-arm
          path: aniworld
